//agente cajero

/*Creencias iniciales*/

at(P) :- pos(P,X,Y) & pos(r1,X,Y). //Creencia inicial de que se encuentra en la posición P

/*Planes iniciales*/
!start.


/*----------------*/

 
+!start : true <- .print("Hola, soy el cajero").
	  
+at(P) : true <- .print("Hola, jejeje").
 
